
# Quick Start

* Install npm dependencies:

    ```shell
    $ npm install
    ```

* Set the environment variables in the .env file

* Run dev server with:

    ```shell
    $ npm start
    ```

    *Port is set to 3000 by default*
